server:
  port: 8081

spring:
  application:
    name: user-service

  # Config Client
  config:
    import: "optional:configserver:http://config-server:8888"




management:
  endpoints:
    web:
      exposure:
        include: "*"    # ← exposer tout pour débugger
  endpoint:
    refresh:
      enabled: true